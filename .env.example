# Database
DATABASE_URL="file:./db/osint-scanner.db"

# App root directory (auto-detected, only set for custom deployments)
# APP_ROOT="/app"

# Python path (default: python3)
# PYTHON_PATH="python3"
